var config = {
    map: {
        '*': {
            tagserve_cookie: 'Flipmediaco_TagServeTracking/js/tagserve_cookie'
        }
    },
    shim: {
        tagserve_cookie: {
            deps: ['jquery']
        }
    }
};